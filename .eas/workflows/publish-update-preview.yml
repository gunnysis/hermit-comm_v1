# Preview ?? OTA ???? ?? (Android ??)
# ???: main ?? ??? push ?? ?? ??
name: Publish Update (Preview)

on:
  push:
    branches:
      - '*'
      - '!main'
  workflow_dispatch: {}

jobs:
  update:
    name: Publish to Preview Channel
    type: update
    environment: preview
    params:
      platform: android
      channel: preview
      message: ${{ github.event.head_commit.message || 'Preview update' }}
