# Preview 채널 OTA 업데이트 배포 (Android 전용 · 수동 실행만)
name: Publish Update (Preview)

on:
  workflow_dispatch: {}

jobs:
  update:
    name: Publish to Preview Channel
    type: update
    environment: preview
    params:
      platform: android
      channel: preview
      message: 'Preview update (manual)'
