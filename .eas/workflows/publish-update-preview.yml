# Preview ì±„ë„ OTA ?…ë°?´íŠ¸ ë°°í¬
# ?¸ë¦¬ê±? main ?œì™¸ ë¸Œëœì¹?push ?ëŠ” ?˜ë™ ?¤í–‰
name: Publish Update (Preview)

on:
  push:
    branches:
      - '*'
      - '!main'
  workflow_dispatch: {}
jobs:
  update:
    name: Publish to Preview Channel
    type: update
    environment: preview
    params:
      channel: preview
      message: ${{ github.event.head_commit.message || 'Preview update' }}
