# Production ??? (?? ?? · Android ??)
# ?? ?? ??? ?? (push ??? Build and Submit ?????? ??+?? ??)
name: Build Production

on:
  workflow_dispatch: {}

jobs:
  build_android:
    name: Build Android (Production)
    type: build
    params:
      platform: android
      profile: production
      message: ${{ github.event.head_commit.message || 'Production build' }}
