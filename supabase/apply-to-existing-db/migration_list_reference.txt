# Supabase CLI "migration list" 참조
# 명령: supabase migration list (프로젝트 루트에서, supabase link 된 상태)

# 이 저장소에 있는 마이그레이션 파일만 적용 대상입니다:
#   001_schema.sql, 002_rls.sql, 003_grants.sql,
#   009_post_analysis.sql, 010_image_attachment.sql, 011_emotion_trend_rpc.sql, 012_group_delete_rls.sql
# (004~008은 이 repo에 없음. 원격에만 있을 수 있음)

# 출력 예시 (연결된 원격 DB 기준):
#   Local | Remote | Time (UTC)
#   ------|--------|------------
#   001   | 001    | ...
#   002   | 002    | ...
#   003   | 003    | ...
#         | 004    | ...   <- 로컬에 파일 없음(원격 이력만 있을 수 있음)
#   ...
#   009   | 009    | ...
#   010   | 010    | ...
#   011   | 011    | ...
#   012   | 012    | ...
#
# 해석:
# - Local에 있고 Remote에도 있으면 → 이미 적용됨. 해당 파일은 다시 적용하지 않음.
# - Local에 있는데 Remote에 없으면 → 아직 미적용. supabase db push 시 적용됨.
# - Remote에만 있는 004~008은 이 프로젝트 마이그레이션 파일이 아니므로 무시.
