# ============================================================
# 은둔마을 — 환경 변수 예시 (.env.example)
# ============================================================
# 이 파일을 복사해 .env 를 만들고 값을 채우세요. .env 는 저장소에 커밋하지 마세요.
# ============================================================

# --- Supabase (필수) ---
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# --- Supabase CLI / 로컬 개발 ---
SUPABASE_ACCESS_TOKEN=
SUPABASE_DB_PASSWORD=

# --- Sentry (선택, 상세: docs/SENTRY.md) ---
# DSN: sentry.io Create Project(React Native) 후 표시되는 값. 로컬·EAS 모두 사용 시 .env 및 EAS Secret에 설정
EXPO_PUBLIC_SENTRY_DSN=
# 소스맵 업로드 사용 시 EAS Secrets에 아래 4개 설정 (SENTRY_DISABLE_AUTO_UPLOAD=false 로 덮어씀)
# SENTRY_ORG=조직슬러그
# SENTRY_PROJECT=프로젝트슬러그
# SENTRY_AUTH_TOKEN=sntrys_xxx
# SENTRY_DISABLE_AUTO_UPLOAD=false

# --- E2E 테스트 (Maestro) ---
MAESTRO_ADMIN_EMAIL=
MAESTRO_ADMIN_PASSWORD=
# MAESTRO_CLEAR_APP_DATA=false
